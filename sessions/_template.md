# Session — YYYY-MM-DD — <slug>

> Written at the **end** of a session. Save as `sessions/YYYY-MM-DD-<slug>.md`
> and commit it alongside the code. The `post-commit` hook replaces the
> `_pending_` below with the real commit hash, linking this log to git history.

- **Feature:** `features/<slug>/IP.md`
- **Commit:** `_pending_`

## What was done

<!-- The concrete changes this session made. -->

## Decisions made

<!-- Non-trivial choices and the reasoning — the "why", not just the "what".
     Future-you (and the next session) reads this first. -->

## What didn't work

<!-- Dead ends, approaches abandoned, and why — so nobody retries them. -->

## What's next

<!-- The very next thing to pick up. A new session should be able to start
     from this line alone. -->
